# Server-side Environment Variables (never exposed to client browser)
DATABASE_URL=postgresql://user:password@localhost:5432/mushak_db
LOG_LEVEL=info

# Client-safe Environment Variables (publicly accessible)
NEXT_PUBLIC_BASE_PATH=
